# Task PRD-0604: Normalize Robot Target Profiles

> PRD: PRD-06 | Depends: PRD-0603  
> Files: 2 | Lines: ~120 | Time: ~30min

## What to Build
Add a robot target adapter that bundles ROS2, network, and safety paths into one profile object.

## Files
### Create: `src/anima_def_cai/connectors/robot_targets.py`
```python
class RobotTargetProfile(BaseModel):
    host: str
    ros_domain_id: int | None = None
```

## Verify
```bash
uv run pytest tests/test_robot_findings.py -v
```

